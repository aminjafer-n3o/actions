name: Auto add issues to projects

on:
  issues:
    types:
      - assigned

jobs:
  add-to-projects:
    uses: aminjafer-n3o/actions/.github/workflows/auto-add-issue-to-projects.yml@main
    secrets: inherit
